The project uses /assets as its default root for streaming content.
 
Add any subfolders you need, such as /videos or /pictures, and by adding stream/{location}, 
the server will stream from /assets/{location}.
